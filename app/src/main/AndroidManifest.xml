<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.geocrat.geeksapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/geek"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ath.SigninActivity" />
        <activity android:name=".ath.LoginActivity" />
        <activity android:name=".note_activity.EditActivity" />
        <activity android:name=".note_activity.NotesDetailActivity" />
        <activity android:name=".note_activity.MainActivity" />
    </application>

</manifest>